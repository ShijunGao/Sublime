Windows API
	面向过程  代码 抽象
(QT)
	C++
	跨平台 (Java)
	接口
	效率低
MFC (Wizards)
	微软基础类库 
	只在Windows
基础：
	窗口
		客户区
	进程 协调
		消息队列
	创建窗口
		资源（不可变、图片、文件）
鼠标动作-消息队列-回掉函数-函数处理（逻辑自定义）
MFC创建:
	exe-about-风格-备注、注释-选类库，动态（在外部，危险）-核实-确定
	CDialog类（以C开头，自带）	
	对话框 
		--对应ID（设置回馈）、注释  
	控件拖拽（标尺）
		-右击属性（属性框固定）
			--标题
	具体逻辑设计：
		为button消息注册;
			右击
				消息映射
					工程、界面、控件-消息映射列表
						添加函数-Edited Code
		控件取到数据
			右击
				消息映射-ID
					添加-输入值、值类型
		应用程序使用变量
			提示访问变量、对变量进行运算
		画面的值更新入UpdateData（bool）


	常用的控件方法
		选中button-设计逻辑--事件处理函数
			返回选中项
			if(){
			return  ;
			}
		更新
			UpdateData(FALSE);
		做函数

		表示框创建变量

		

